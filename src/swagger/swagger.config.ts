export interface SwaggerConfig {
  title: string;
  description: string;
  version: string;
}

export const SWAGGER_CONFIG: SwaggerConfig = {
  title: 'VOTING-NEST-API',
  description: 'the description of the API',
  version: '1.0.0',
};
